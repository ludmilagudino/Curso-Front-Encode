// VARIABLES

// VAR: alcance a nivel de function, si no se define toma valor UNDIFINE
// LET: alcance a nivel de bloque, si no se asigna manda error re referencia
// CONST: no pueden ser reasignadas

let numeros = [2, 5, 6]; // arreglo
let alumno = {nombre: 'Juan', edad: 24} //objeto